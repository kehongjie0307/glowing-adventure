<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PM Nexus - 产品经理全链路工作台</title>
    <!-- React & ReactDOM -->
    <script crossorigin src="https://unpkg.com/react@18/umd/react.production.min.js"></script>
    <script crossorigin src="https://unpkg.com/react-dom@18/umd/react-dom.production.min.js"></script>
    <!-- Babel for JSX -->
    <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
    <!-- Ant Design CSS (v4 for stability in CDN) -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/antd/4.24.15/antd.min.css"/>
    <!-- Ant Design JS -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/moment.js/2.29.4/moment.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/antd/4.24.15/antd.min.js"></script>
    <!-- Ant Design Icons -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/ant-design-icons/4.7.0/index.umd.min.js"></script>
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    
    <style>
        body { margin: 0; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, 'Noto Sans', sans-serif; }
        .site-layout .site-layout-background { background: #fff; }
        .logo {
            height: 32px;
            margin: 16px;
            background: rgba(255, 255, 255, 0.2);
            color: white;
            font-weight: bold;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 4px;
            font-size: 16px;
            letter-spacing: 1px;
        }
        /* Custom scrollbar for realistic app feel */
        ::-webkit-scrollbar { width: 6px; height: 6px; }
        ::-webkit-scrollbar-thumb { background: #ccc; border-radius: 3px; }
        ::-webkit-scrollbar-track { background: transparent; }
        
        .card-hover:hover {
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
            transition: all 0.3s ease;
        }
    </style>
</head>
<body>
    <div id="root"></div>

    <script type="text/babel">
        // Added Badge to the destructuring list
        const { Layout, Menu, Breadcrumb, Card, Row, Col, Statistic, Table, Tag, Button, Input, Tabs, Timeline, Steps, Avatar, List, Divider, Empty, Spin, message, Modal, Drawer, Tooltip, Progress, Badge } = antd;
        const { Header, Content, Footer, Sider } = Layout;
        const { Search, TextArea } = Input;
        const { TabPane } = Tabs;
        const { Step } = Steps;
        
        // Consolidated all icons here to avoid reference errors
        const { 
            DashboardOutlined, 
            BulbOutlined, 
            FormOutlined, 
            ToolOutlined, 
            FileSearchOutlined, 
            TeamOutlined, 
            AimOutlined, 
            CompassOutlined, 
            FileTextOutlined, 
            PartitionOutlined, 
            MobileOutlined, 
            RocketOutlined, 
            PlusOutlined,
            RobotOutlined,
            UserOutlined,
            MoreOutlined,
            CheckCircleOutlined,
            ClockCircleOutlined,
            SoundOutlined,
            FolderOpenOutlined, 
            SettingOutlined, 
            MenuUnfoldOutlined, 
            BoldOutlined, 
            PictureOutlined, 
            BellOutlined 
        } = icons; 

        const { useState, useEffect } = React;

        // --- Mock Data ---
        const recentDocs = [
            { key: '1', name: 'V2.0 需求调研报告', type: '调研文档', status: '已完成', date: '2024-05-20', owner: 'Alex' },
            { key: '2', name: '首页改版竞品分析', type: '竞品分析', status: '进行中', date: '2024-05-21', owner: 'Sarah' },
            { key: '3', name: '需求评审会议纪要-0522', type: '会议纪要', status: '待整理', date: '2024-05-22', owner: 'Mike' },
            { key: '4', name: '电商模块 PRD v1.0', type: 'PRD文档', status: '评审中', date: '2024-05-23', owner: 'Alex' },
        ];

        // --- Components ---

        // 1. Dashboard (工作台)
        const Dashboard = () => (
            <div className="space-y-6 animate-fade-in">
                <div className="flex justify-between items-center mb-4">
                    <h2 className="text-2xl font-bold text-gray-800">早安，产品经理</h2>
                    <Button type="primary" icon={<PlusOutlined />} size="large">新建项目</Button>
                </div>

                <Row gutter={16}>
                    <Col span={6}>
                        <Card bordered={false} className="card-hover">
                            <Statistic title="进行中项目" value={12} prefix={<RocketOutlined className="text-blue-500"/>} />
                        </Card>
                    </Col>
                    <Col span={6}>
                        <Card bordered={false} className="card-hover">
                            <Statistic title="本周产出物" value={28} prefix={<FileTextOutlined className="text-green-500"/>} />
                        </Card>
                    </Col>
                    <Col span={6}>
                        <Card bordered={false} className="card-hover">
                            <Statistic title="待评审需求" value={5} prefix={<TeamOutlined className="text-orange-500"/>} />
                        </Card>
                    </Col>
                    <Col span={6}>
                        <Card bordered={false} className="card-hover">
                            <Statistic title="AI 节省时间 (h)" value={42} prefix={<RobotOutlined className="text-purple-500"/>} precision={1} />
                        </Card>
                    </Col>
                </Row>

                <Row gutter={16}>
                    <Col span={16}>
                        <Card title="最近产出物" extra={<a href="#">查看全部</a>} bordered={false} className="h-full">
                            <Table 
                                columns={[
                                    { title: '文档名称', dataIndex: 'name', key: 'name', render: text => <a className="font-medium">{text}</a> },
                                    { title: '类型', dataIndex: 'type', key: 'type', render: type => <Tag color={type === 'PRD文档' ? 'blue' : type === '调研文档' ? 'cyan' : 'orange'}>{type}</Tag> },
                                    { title: '状态', dataIndex: 'status', key: 'status', render: status => <Tag icon={status === '已完成' ? <CheckCircleOutlined /> : <ClockCircleOutlined />} color={status === '已完成' ? 'success' : 'processing'}>{status}</Tag> },
                                    { title: '更新时间', dataIndex: 'date', key: 'date', className: 'text-gray-400' },
                                ]}
                                dataSource={recentDocs}
                                pagination={false}
                                size="small"
                            />
                        </Card>
                    </Col>
                    <Col span={8}>
                        <Card title="AI 助手建议" bordered={false} className="h-full">
                            <List
                                itemLayout="horizontal"
                                dataSource={[
                                    { title: '完善用户画像', desc: '根据最新的调研数据，建议更新 User Persona A。' },
                                    { title: '竞品动态更新', desc: '监测到竞品 X 上线了新功能，建议查看分析报告。' },
                                    { title: '原型一致性检查', desc: '检测到 PRD 中的登录逻辑与原型不符。' },
                                ]}
                                renderItem={item => (
                                    <List.Item>
                                        <List.Item.Meta
                                            avatar={<Avatar style={{ backgroundColor: '#87d068' }} icon={<RobotOutlined />} />}
                                            title={<a href="#">{item.title}</a>}
                                            description={item.desc}
                                        />
                                    </List.Item>
                                )}
                            />
                        </Card>
                    </Col>
                </Row>
            </div>
        );

        // 2. Meeting Notes (会议纪要)
        const MeetingNotes = () => {
            const [analyzing, setAnalyzing] = useState(false);
            const [result, setResult] = useState(null);

            const handleAnalyze = () => {
                setAnalyzing(true);
                setTimeout(() => {
                    setAnalyzing(false);
                    setResult(true);
                    message.success('AI 已完成纪要提取与需求转化！');
                }, 1500);
            };

            return (
                <div className="bg-white p-6 rounded-lg shadow-sm h-full">
                    <div className="flex justify-between items-center mb-6">
                        <div>
                            <Breadcrumb>
                                <Breadcrumb.Item>构想中心</Breadcrumb.Item>
                                <Breadcrumb.Item>会议纪要</Breadcrumb.Item>
                            </Breadcrumb>
                            <h2 className="text-2xl font-bold mt-2">需求评审会-0524</h2>
                        </div>
                        <div className="space-x-3">
                            <Tag icon={<SoundOutlined />} color="processing">正在录音 00:45:12</Tag>
                            <Button type="primary" icon={<RobotOutlined />} onClick={handleAnalyze} loading={analyzing}>
                                AI 智能提取
                            </Button>
                        </div>
                    </div>

                    <Row gutter={24} className="h-full">
                        <Col span={14} className="border-r pr-6">
                            <h3 className="font-bold text-gray-500 mb-4 uppercase text-xs">实时转写</h3>
                            <div className="bg-gray-50 p-4 rounded-lg h-96 overflow-y-auto space-y-4">
                                <div className="flex gap-3">
                                    <Avatar src="https://i.pravatar.cc/150?u=1" />
                                    <div>
                                        <div className="text-xs text-gray-500 mb-1">产品经理 Alex 14:02</div>
                                        <div className="bg-white p-3 rounded-lg shadow-sm text-sm">我们要重点讨论一下这次的“一键生成 Demo”功能。</div>
                                    </div>
                                </div>
                                <div className="flex gap-3">
                                    <Avatar src="https://i.pravatar.cc/150?u=2" />
                                    <div>
                                        <div className="text-xs text-gray-500 mb-1">研发 Leader Mike 14:03</div>
                                        <div className="bg-white p-3 rounded-lg shadow-sm text-sm">目前技术上可以通过 Tailwind UI 的映射来实现，但是 Mock 数据的真实性是个问题。</div>
                                    </div>
                                </div>
                                <div className="flex gap-3">
                                    <Avatar src="https://i.pravatar.cc/150?u=1" />
                                    <div>
                                        <div className="text-xs text-gray-500 mb-1">产品经理 Alex 14:04</div>
                                        <div className="bg-white p-3 rounded-lg shadow-sm text-sm">好的，那我们先定下来，第一版先用随机数据，后续接入 LLM 生成真实数据。这一点记下来。</div>
                                    </div>
                                </div>
                                {/* More mock items */}
                            </div>
                        </Col>
                        <Col span={10}>
                            <h3 className="font-bold text-gray-500 mb-4 uppercase text-xs flex items-center">
                                <RobotOutlined className="mr-2 text-indigo-500"/> AI 提炼结果
                            </h3>
                            {result ? (
                                <div className="space-y-4 animate-fade-in-up">
                                    <Card size="small" title="待办事项 (TODO)" className="border-l-4 border-indigo-500">
                                        <Timeline className="mt-4">
                                            <Timeline.Item color="green">确定 Demo 生成使用随机 Mock 数据方案</Timeline.Item>
                                            <Timeline.Item color="blue">Mike 调研 Tailwind UI 组件库覆盖率</Timeline.Item>
                                            <Timeline.Item>下周三前输出技术方案文档</Timeline.Item>
                                        </Timeline>
                                    </Card>
                                    <Card size="small" title="已转化需求" className="border-l-4 border-green-500">
                                        <div className="space-y-2">
                                            <div className="flex justify-between items-center p-2 bg-gray-50 rounded">
                                                <span className="text-sm font-medium">Req-102: Demo 数据 Mock 模块</span>
                                                <Tag color="purple">P1</Tag>
                                            </div>
                                            <div className="flex justify-between items-center p-2 bg-gray-50 rounded">
                                                <span className="text-sm font-medium">Req-103: 组件映射规则定义</span>
                                                <Tag color="red">P0</Tag>
                                            </div>
                                        </div>
                                    </Card>
                                </div>
                            ) : (
                                <Empty description="点击“AI 智能提取”生成结构化纪要" image={Empty.PRESENTED_IMAGE_SIMPLE} />
                            )}
                        </Col>
                    </Row>
                </div>
            );
        };

        // 3. User Journey (用户旅程)
        const UserJourney = () => {
            return (
                <div className="space-y-6">
                    <div className="flex justify-between items-center">
                         <div>
                            <Breadcrumb>
                                <Breadcrumb.Item>定义中心</Breadcrumb.Item>
                                <Breadcrumb.Item>用户旅程地图</Breadcrumb.Item>
                            </Breadcrumb>
                            <h2 className="text-2xl font-bold mt-2">新用户注册与激活流程</h2>
                        </div>
                        <Button type="primary" icon={<RobotOutlined />}>AI 优化旅程</Button>
                    </div>

                    <div className="bg-white p-8 rounded-lg shadow-sm overflow-x-auto">
                         <Steps progressDot current={1} className="mb-12">
                            <Step title="认知阶段" description="看到广告/朋友推荐" />
                            <Step title="考虑阶段" description="浏览官网/下载App" />
                            <Step title="行动阶段" description="注册/首次使用" />
                            <Step title="留存阶段" description="完成核心任务" />
                        </Steps>

                        <div className="grid grid-cols-6 gap-4 border-t pt-6 min-w-[800px]">
                            {/* Header Column */}
                            <div className="col-span-1 space-y-12 pt-12 font-bold text-gray-500 text-right pr-4 text-sm">
                                <div>用户行为</div>
                                <div>情绪曲线</div>
                                <div>痛点</div>
                                <div>机会点</div>
                            </div>

                            {/* Phase Columns */}
                            {['认知', '考虑', '行动', '留存', '推荐'].map((phase, idx) => (
                                <div key={idx} className="col-span-1 space-y-8 relative border-l pl-4">
                                    <div className="text-xs font-bold text-indigo-500 bg-indigo-50 inline-block px-2 py-1 rounded mb-4">{phase}</div>
                                    
                                    {/* Actions */}
                                    <div className="h-20 text-sm text-gray-600">
                                        {idx === 2 ? '输入手机号\n接收验证码\n设置密码' : '浏览页面\n查看评价'}
                                    </div>

                                    {/* Emotion - Simplified visual */}
                                    <div className="h-16 flex items-center">
                                        <div className="w-full h-1 bg-gray-200 relative">
                                            <div className="absolute w-3 h-3 rounded-full bg-yellow-500 border-2 border-white shadow" style={{top: -4, left: '50%', transform: `translateY(${idx === 2 ? 10 : -10}px)`}}></div>
                                        </div>
                                    </div>

                                    {/* Pain Points */}
                                    <div className="h-24">
                                        {idx === 2 && (
                                            <div className="bg-red-50 text-red-600 p-2 rounded text-xs border border-red-100">
                                                <i className="fas fa-exclamation-circle mr-1"></i>
                                                验证码延迟高，用户易流失
                                            </div>
                                        )}
                                    </div>

                                    {/* Opportunities */}
                                    <div>
                                        {idx === 2 && (
                                            <Button type="dashed" size="small" className="text-xs w-full text-indigo-600 border-indigo-300">
                                                + 转为需求: 本机号码一键登录
                                            </Button>
                                        )}
                                    </div>
                                </div>
                            ))}
                        </div>
                    </div>
                </div>
            );
        };

        // 4. Intelligent PRD (智能PRD)
        const IntelligentPRD = () => {
            return (
                <div className="h-full flex flex-col bg-white rounded-lg shadow-sm">
                    {/* Toolbar */}
                    <div className="border-b p-3 flex items-center gap-2 bg-gray-50 rounded-t-lg">
                        <Button type="text" icon={<MenuUnfoldOutlined />} />
                        <Divider type="vertical" />
                        <Button>H1</Button>
                        <Button>H2</Button>
                        <Button icon={<BoldOutlined />} />
                        <Divider type="vertical" />
                        <Button type="primary" ghost icon={<RobotOutlined />}>AI 续写</Button>
                        <Button icon={<PictureOutlined />}>插入三图一表</Button>
                        <div className="flex-1"></div>
                        <Tag color="blue">自动保存于 14:23</Tag>
                        <Button type="primary">导出</Button>
                    </div>
                    
                    {/* Content */}
                    <div className="flex-1 flex overflow-hidden">
                        {/* Outline */}
                        <div className="w-64 border-r p-4 bg-gray-50 hidden md:block">
                            <h4 className="font-bold text-gray-600 mb-4 text-xs uppercase">文档大纲</h4>
                            <ul className="space-y-2 text-sm text-gray-600">
                                <li className="font-bold text-indigo-600">1. 项目背景</li>
                                <li className="pl-4 hover:text-indigo-600 cursor-pointer">1.1 业务目标</li>
                                <li className="pl-4 hover:text-indigo-600 cursor-pointer">1.2 用户范围</li>
                                <li className="hover:text-indigo-600 cursor-pointer">2. 功能需求</li>
                                <li className="pl-4 hover:text-indigo-600 cursor-pointer">2.1 登录注册</li>
                                <li className="pl-4 hover:text-indigo-600 cursor-pointer">2.2 首页推荐</li>
                                <li className="hover:text-indigo-600 cursor-pointer">3. 数据埋点</li>
                            </ul>
                        </div>

                        {/* Editor Area */}
                        <div className="flex-1 p-8 overflow-y-auto max-w-4xl mx-auto w-full">
                            <h1 className="text-4xl font-bold mb-6">PM Nexus V2.0 产品需求文档</h1>
                            
                            <div className="mb-8">
                                <h2 className="text-2xl font-bold mb-4 border-l-4 border-indigo-500 pl-3">1. 项目背景</h2>
                                <p className="text-gray-600 leading-relaxed mb-4">
                                    为了解决产品经理在日常工作中产出物割裂、重复劳动多、缺乏可视化等痛点，我们计划开发 PM Nexus V2.0 版本。
                                    该版本将重点引入 AI 能力，打通从“会议纪要”到“PRD”再到“Demo”的全链路。
                                </p>
                                <div className="bg-blue-50 p-4 rounded-lg border border-blue-100 flex items-start gap-3">
                                    <RobotOutlined className="text-blue-500 mt-1" />
                                    <div>
                                        <div className="text-sm font-bold text-blue-700 mb-1">AI 调研洞察引用</div>
                                        <div className="text-sm text-blue-600">根据《2024产品经理生存现状调研》，85% 的 PM 认为编写文档占用了超过 40% 的工作时间。</div>
                                    </div>
                                </div>
                            </div>

                            <div className="mb-8">
                                <h2 className="text-2xl font-bold mb-4 border-l-4 border-indigo-500 pl-3">2. 核心流程图</h2>
                                <div className="border rounded p-4 bg-gray-50 text-center">
                                    <div className="text-xs text-gray-400 mb-2">Mermaid 渲染组件</div>
                                    <div className="inline-block p-4 bg-white border shadow-sm rounded">
                                        [会议录音] --> [AI 提取] --> [PRD 草稿] --> [自动生成原型]
                                    </div>
                                </div>
                            </div>

                            <div className="mb-8">
                                <h2 className="text-2xl font-bold mb-4 border-l-4 border-indigo-500 pl-3">3. 功能详细设计</h2>
                                <Table 
                                    size="small"
                                    columns={[
                                        { title: '字段', dataIndex: 'field', width: 150 },
                                        { title: '类型', dataIndex: 'type', width: 100 },
                                        { title: '描述/规则', dataIndex: 'desc' },
                                    ]}
                                    dataSource={[
                                        { field: 'ProjectName', type: 'String', desc: '项目名称，必填，最长 50 字符' },
                                        { field: 'Status', type: 'Enum', desc: '状态：进行中、已归档、草稿' },
                                    ]}
                                    pagination={false}
                                    bordered
                                />
                            </div>
                        </div>
                    </div>
                </div>
            );
        };

        // --- Main Layout ---
        const App = () => {
            const [collapsed, setCollapsed] = useState(false);
            const [currentView, setCurrentView] = useState('dashboard');

            // Icon Mapping Hack for non-destructured Icons
            const Icons = {
                Dashboard: DashboardOutlined,
                Ideation: BulbOutlined,
                Definition: FormOutlined,
                Production: ToolOutlined
            };

            const renderContent = () => {
                switch(currentView) {
                    case 'dashboard': return <Dashboard />;
                    case 'meeting': return <MeetingNotes />;
                    case 'journey': return <UserJourney />;
                    case 'prd': return <IntelligentPRD />;
                    default: return <div className="flex items-center justify-center h-full text-gray-400">模块开发中...</div>;
                }
            };

            return (
                <Layout style={{ minHeight: '100vh' }}>
                    <Sider collapsible collapsed={collapsed} onCollapse={setCollapsed} width={240} className="shadow-lg z-10">
                        <div className="logo">
                            <i className="fas fa-layer-group mr-2"></i>
                            {!collapsed && "PM Nexus"}
                        </div>
                        <Menu theme="dark" defaultSelectedKeys={['1']} mode="inline" onClick={(e) => setCurrentView(e.key)}>
                            <Menu.Item key="dashboard" icon={<DashboardOutlined />}>工作台</Menu.Item>
                            
                            <Menu.SubMenu key="sub1" icon={<BulbOutlined />} title="构想中心">
                                <Menu.Item key="research">调研文档</Menu.Item>
                                <Menu.Item key="competitor">竞品分析</Menu.Item>
                                <Menu.Item key="meeting">会议纪要</Menu.Item>
                            </Menu.SubMenu>
                            
                            <Menu.SubMenu key="sub2" icon={<FormOutlined />} title="定义中心">
                                <Menu.Item key="journey">用户旅程</Menu.Item>
                                <Menu.Item key="prd">智能 PRD</Menu.Item>
                                <Menu.Item key="charts">三图一表</Menu.Item>
                            </Menu.SubMenu>
                            
                            <Menu.SubMenu key="sub3" icon={<ToolOutlined />} title="制作中心">
                                <Menu.Item key="prototype">产品原型</Menu.Item>
                                <Menu.Item key="demo">演示 Demo</Menu.Item>
                            </Menu.SubMenu>
                            
                            <Menu.Divider />
                            <Menu.Item key="assets" icon={<FolderOpenOutlined />}>资产库</Menu.Item>
                            <Menu.Item key="settings" icon={<SettingOutlined />}>设置</Menu.Item>
                        </Menu>
                    </Sider>
                    
                    <Layout className="site-layout bg-gray-100">
                        <Header className="site-layout-background px-6 flex justify-between items-center shadow-sm z-10" style={{ padding: 0, background: '#fff' }}>
                            <div className="px-6 flex items-center">
                                <h3 className="mb-0 font-bold text-lg text-gray-700">
                                    {currentView === 'dashboard' ? '工作台' : 
                                     currentView === 'meeting' ? '会议纪要' :
                                     currentView === 'journey' ? '用户旅程地图' :
                                     currentView === 'prd' ? '智能 PRD 编辑器' : 'PM Nexus'}
                                </h3>
                            </div>
                            <div className="px-6 flex items-center gap-4">
                                <Input.Search placeholder="搜索全站资源..." style={{ width: 200 }} />
                                <Badge count={3}>
                                    <BellOutlined style={{ fontSize: '18px', cursor: 'pointer' }} />
                                </Badge>
                                <Avatar style={{ backgroundColor: '#4f46e5' }} icon={<UserOutlined />} />
                            </div>
                        </Header>
                        
                        <Content style={{ margin: '16px' }}>
                            {renderContent()}
                        </Content>
                        
                        <Footer style={{ textAlign: 'center', color: '#999' }}>PM Nexus ©2024 Created by Product Team</Footer>
                    </Layout>
                </Layout>
            );
        };

        ReactDOM.createRoot(document.getElementById('root')).render(<App />);
    </script>
</body>
</html>